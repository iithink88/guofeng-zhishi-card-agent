# 国风知识卡片 · 智能体

将任意知识点转化为**中国风传统主题插画风格**的知识卡片。宣纸为底、朱印落款、楷体标题，AI 生成国风插画与文案，一键导出自包含 HTML。

## 来源

由扣子(Coze)工作流「知识卡片_中国风传统主题插画风格」转换而来。原工作流的「图片提示词」节点 systemPrompt（国风插画提示词生成核心）已原样移植为 `COZE_IMAGE_SYSTEM` 常量，保证国风插画提示词的专业性。

## 快速开始

1. 确保已安装 [Node.js](https://nodejs.org)（LTS 版，勾选 Add to PATH）
2. 双击 `启动.bat`，浏览器自动打开 `http://127.0.0.1:8788`
3. 点击右上角 ⚙ 设置：
   - 选择 LLM 服务商（DeepSeek / 通义千问 / 智谱 / Kimi / OpenAI 等），填入 API Key
   - （可选）配置图像模型，生成真正的国风 AI 插画
4. 输入知识点（如「二十四节气」「勾股定理」），选择学段，点击「开始生成」

## 不填 Key 也能用

未配置任何模型时，内置国风简笔占位插画 + SVG 图标库可生成完整卡片，零成本、秒出图。

## 功能

- **国风卡片排版**：宣纸底色、朱红印章、楷体标题、墨色正文、留白构图
- **AI 文案**：LLM 生成知识点释义、要点、口诀金句，适配小学/初中/高中
- **AI 国风插画**：Coze 原版 systemPrompt 驱动，支持工笔/水墨/年画/青绿山水等风格
- **多服务商预设**：9 家 LLM + 4 家图模型一键填充
- **自包含导出**：生成的 HTML 单文件，离线可看、可打印

## 文件结构

```
国风知识卡片智能体/
├── 启动.bat          # 主启动器（自动探测 Node）
├── 启动.vbs          # 备用启动器
├── 修复bat关联.reg    # 修复 .bat 文件关联
├── server.js         # 零依赖 Node 后端
├── index.html        # 前端页面
├── css/styles.css    # 国风主题样式
├── js/app.js         # 前端逻辑
├── .env.example      # 配置模板（不含真实 Key）
├── 使用说明.md       # 详细使用说明
├── 启动日志.txt      # 运行日志
└── output/           # 生成的卡片存放目录
```
